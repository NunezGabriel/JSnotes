// INTERACCION CON JSON EN GERAL EN JS
/*
    Aca veremos como podemos interactuar con archivos JSON que tengamos en local desde JS con metodos de parseo etc y para traerlos usaremos fetch y para trabajar en base a esos datos usaremos lo metodos que veremos a continuacion
*/
//////////////////////////////////////////

//JSON.parse() ... este metodo al usar fecth con toda la logica que vimos no es necesario si en el fecth estas aplicando un .json() ya que este metodo aparte de extraer los datos les hace un parse por default :)